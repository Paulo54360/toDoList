import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maToDoList.app',
  appName: 'maToDoList',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
